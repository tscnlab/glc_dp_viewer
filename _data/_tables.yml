- id: study_schema
  required: true

- id: contributor_schema
  required: false

- id: dataset_schema
  required: true

- id: device_schema
  required: true

- id: device_datasheet_schema
  required: true

- id: participant_schema
  required: true

- id: participant_characteristics_schema
  required: false

- id: light_data_schema.json
  example: true
